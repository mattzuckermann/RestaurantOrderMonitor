<li>
	<div class="row">
		<div class="col-6">
			{{burger_name}}
		</div>
		<div class="col-6">
			{{#if hungry}}
			<button class="change-devoured" data-id="{{id}}" data-neweaten="{{hungry}}">
				DEVOUR IT!
			</button>
			{{else}} {{/if}}
		</div>
	</div>
</li>